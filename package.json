{
  "name": "the-gear-of-seasons",
  "version": "2.0.0",
  "description": "The heaet of hexe.net",
  "scripts": {
    "test": "(cd bridge && npm run test)",
    "up": "(cd bridge && npm run up) && (cd server && npm run up) && (cd client && npm run up)"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fairy-rockets/the-gear-of-seasons.git"
  },
  "author": "Kaede Fujisaki",
  "license": "AGPL-3.0-or-later",
  "bugs": {
    "url": "https://github.com/fairy-rockets/the-gear-of-seasons/issues"
  },
  "homepage": "https://github.com/fairy-rockets/the-gear-of-seasons#readme",
  "workspaces": [
    "bridge",
    "client",
    "server"
  ],
  "dependencies": {
    "dayjs": "^1.10.5",
    "generic-pool": "^3.7.8",
    "ts-postgres": "^1.1.3"
  },
  "devDependencies": {
    "@types/generic-pool": "^3.1.9"
  }
}
