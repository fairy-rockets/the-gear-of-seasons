import World from "./World";

export default class Gear {
  /**
   * @param {World} world 
   */
  constructor(world) {
    this.world_ = world;
  }
  /**
   * 
   * @param {WebGLRenderingContext} gl 
   */
  render(gl) {

  }
}